<ui:FluentWindow x:Class="StartupGroups.App.Views.UpdateFlyoutWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:conv="clr-namespace:StartupGroups.App.Converters"
                 xmlns:ctrl="clr-namespace:StartupGroups.App.Controls"
                 xmlns:loc="clr-namespace:StartupGroups.App.Localization"
                 Title="{loc:Translate UpdateFlyout_Header_UpdateAvailable}"
                 Width="640"
                 Height="600"
                 MinWidth="520"
                 MinHeight="460"
                 FlowDirection="{loc:FlowDirection}"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="CanResize"
                 ShowInTaskbar="False"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round">
    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <conv:BoolToVisibilityConverter x:Key="NotBoolToVisibility" Invert="True" />
    </Window.Resources>
    <Window.InputBindings>
        <!-- ESC dismisses the flyout (same as clicking Later). DeferCommand
             gates on IsDownloading so a mid-download ESC is a no-op rather
             than abandoning the download. -->
        <KeyBinding Key="Escape" Command="{Binding DeferCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{loc:Translate UpdateFlyout_Header_UpdateAvailable}" />

        <!-- Hero header -->
        <Border Grid.Row="1" Padding="24,16,24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0"
                        Width="56" Height="56"
                        CornerRadius="{StaticResource RadiusMd}"
                        Background="{DynamicResource AccentFillColorDefaultBrush}"
                        VerticalAlignment="Top">
                    <ui:SymbolIcon Symbol="ArrowDownload24"
                                   FontSize="32"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                </Border>
                <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding HeaderTitle}" Style="{StaticResource AppH2}" />
                    <TextBlock Text="{Binding HeaderSubtitle}"
                               Style="{StaticResource AppMuted}"
                               Margin="0,4,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Release notes -->
        <Border Grid.Row="2" Padding="24,4,24,4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Text="{loc:Translate UpdateFlyout_ReleaseNotes_Heading}"
                           Style="{StaticResource AppH3}"
                           Margin="0,0,0,8" />
                <Border Grid.Row="1"
                        CornerRadius="{StaticResource RadiusMd}"
                        Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        Padding="14,10">
                    <ctrl:MarkdownView Markdown="{Binding ReleaseNotesMarkdown}"
                                       Background="Transparent"
                                       BorderThickness="0"
                                       VerticalScrollBarVisibility="Auto"
                                       HorizontalScrollBarVisibility="Disabled"
                                       IsToolBarVisible="False" />
                </Border>
            </Grid>
        </Border>

        <!-- Action bar — no Background so Mica shows through. -->
        <Border Grid.Row="3"
                Padding="24,16,24,20">
            <StackPanel>
                <!-- Progress (visible while downloading) -->
                <StackPanel Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisibility}}"
                            Margin="0,0,0,12">
                    <TextBlock Text="{Binding DownloadingLabel}"
                               Style="{StaticResource AppMuted}"
                               Margin="0,0,0,6" />
                    <ProgressBar Value="{Binding DownloadProgress, Mode=OneWay}"
                                 Maximum="1"
                                 Height="14"
                                 HorizontalAlignment="Stretch"
                                 IsIndeterminate="False" />
                    <Grid Margin="0,6,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{Binding ProgressText}"
                                   FontSize="{StaticResource FontSizeMuted}"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBlock Grid.Column="1"
                                   Text="{Binding SpeedText}"
                                   FontSize="{StaticResource FontSizeMuted}"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </Grid>
                </StackPanel>

                <!-- Failure message -->
                <Border Visibility="{Binding HasFailed, Converter={StaticResource BoolToVisibility}}"
                        CornerRadius="{StaticResource RadiusSm}"
                        Background="{DynamicResource SystemFillColorCriticalBackgroundBrush}"
                        BorderBrush="{DynamicResource SystemFillColorCriticalBrush}"
                        BorderThickness="1"
                        Padding="12,8"
                        Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="ErrorCircle24"
                                       Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                       Margin="0,0,8,0"
                                       VerticalAlignment="Center" />
                        <TextBlock Text="{Binding FailureMessage}"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                    </StackPanel>
                </Border>

                <!-- Buttons -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:HyperlinkButton Grid.Column="0"
                                        Content="{loc:Translate UpdateFlyout_ViewOnGitHub}"
                                        Command="{Binding OpenReleaseNotesCommand}"
                                        VerticalAlignment="Center" />
                    <ui:Button Grid.Column="2"
                               Content="{Binding DeferButtonLabel}"
                               Command="{Binding DeferCommand}"
                               IsEnabled="{Binding CanDefer}"
                               Margin="0,0,8,0" />
                    <ui:Button Grid.Column="3"
                               Content="{Binding InstallButtonLabel}"
                               Command="{Binding InstallCommand}"
                               Appearance="Primary"
                               Icon="{ui:SymbolIcon ArrowDownload24}"
                               IsDefault="True" />
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
